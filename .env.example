# Пример файла .env.example для LLM Telegram-ассистента

# Токен Telegram-бота (обязательный)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# Путь к файлу логов (по умолчанию logs/bot.log)
LOG_FILE_PATH=logs/bot.log

# Путь к базе данных (по умолчанию SQLite)
DB_PATH=data/contacts.db

# Уровень логирования: DEBUG, INFO, WARNING, ERROR (по умолчанию INFO)
LOG_LEVEL=INFO

# Строка подключения к PostgreSQL (опционально)
POSTGRES_DSN=postgresql://user:password@host:port/dbname

# Строка подключения к MySQL (опционально)
MYSQL_DSN=mysql://user:password@host:port/dbname

# Строка подключения к MongoDB (опционально)
MONGO_URI=mongodb://user:password@host:port/dbname

# URL и ключ для облачного каталога услуг (опционально)
CATALOG_API_URL=https://example.com/api/services
CATALOG_API_KEY=your-api-key